<section class="breadcrumb-wrapper">
  <div class="container">
    <h2>Examples</h2>
  </div>
</section>
<div class="container">
  
  <div style="padding:0;">
    <div>
      <br><br>
      <div class="container">
        <div class="col-md-12 text-center"><%= link_to "Add New Example", new_example_path, class: "btn btn-success btn-block" %></div>
        <div class="container-fluid">
          <div class="table-responsive">
            <table class="table table-condensed">
              <thead>
                <tr>
                  <th>Title</th>
                  <th>Campaign</th>
                  <th>Edit</th>
                  <th>More Info</th>
                </tr>
              </thead>
              <tbody>
                <% @examples.each do |example| %>
                  <tr>
                    <td>
                      <%= example.title %>
                    </td>
                    <td>
                      <%= example.campaign.title %>
                    </td>
                    <td>
                        <%= link_to "Edit", edit_example_path(example), class: "btn btn-info" %>
                    </td>
                    <td>
                        <%= link_to "View", example_path(example), class: 'btn btn-primary' %>
                    </td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div>

      </div>
    </div>
  </div>

</div>